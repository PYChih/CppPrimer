/*
- 練習6.18: 為下列每個函式撰寫宣告。當你編寫這些宣告時，使用函式名稱來表明函式所做的事。
```
//a
// 一個名為compare的函式，它會回傳一個bool並有兩個參數，這兩個參數都是對名為matrix的類別的參考
bool compare( const matrix &a, const matrix&b);
//b
// 一個名為change_val的函式，它會回傳一vector<int>迭代器，並接受兩個參數: 其一是一個int，而另一個是某個vector<int>的迭代器
vector<int>::iterator change_val(int i, vector<int>::iterator)
```
 */
#include<iostream>
#include<string>
#include<vector>
using namespace std;

int main(){

    return 0;
}